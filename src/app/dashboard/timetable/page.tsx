import React from 'react';
import TimetableUI from '@/app/components/TimetableUI';

const TimetablePage = () => {
  return (
    <main className="min-h-screen bg-gray-900 p-8">
      <TimetableUI />
    </main>
  );
};

export default TimetablePage;
